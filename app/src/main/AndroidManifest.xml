<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.globant.fernandoraviola.fidreader"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <uses-sdk
        tools:node="replace" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".activities.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.globant.fernandoraviola.fidreader.activities.FeedActivity"
            android:label="@string/title_activity_feed"
            android:parentActivityName="com.globant.fernandoraviola.fidreader.activities.MainActivity">
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value="com.globant.fernandoraviola.fidreader.activities.MainActivity" />
        </activity>
    </application>

</manifest>
